<h1><%= @post.title %></h1>

<h3>Author: <%= link_to @post.blogger.name, blogger_path(@post.blogger) %></h3>
<h3>Destination: <%= link_to @post.destination.name, destination_path(@post.destination) %></h3>

<h3> Content </h3>
<p><%= @post.content %></p>

<h4>Likes: <%= @post.likes %></h4>

<h5>Don't touch</h5>
<%= button_to "Like post", likes_path(@post), method: :patch %><br>

<%= form_for @post do |f| %>
    <%= f.hidden_field :likes, value: @post.like %>

    <%= f.submit "Like Post"%>
<% end %>
